# NIHONGA UI/UX 优化交付

2026-09-01 · 基于用户提供的 `NIHONGA完整项目_20260901_023117.zip`。

这是现有项目的增量优化，保留原生 HTML/CSS/JavaScript、Vercel API、Supabase、现有账号系统及管理后台。没有迁移框架，没有导入或改写线上数据，没有接入 AI，没有发布到生产环境。

## 页面与功能

- 首页依次为搜索、Featured、Recently Added、随机发现、ALL ARTISTS、排行榜与小型统计、About、Contribute。
- 纸白、墨灰和单一岩绿；复用现有《松林図屏風》及其版权标注。
- 艺术家姓名、已有罗马字、有效账号、标签、学校、地区按有无显示。无作品图片时使用姓名字样占位，不使用他人作品或生成图冒充艺术家作品。
- 四位 Featured 优先使用手工标记，其余按东京日期从真实数据中稳定轮换，明确注明每日轮换，不使用排名数据充当推荐。
- Random Discovery 从已加载数组抽取，不重新查询数据库，有多个作家时避免连续抽到同一人。
- 最近加入只使用 `addedAt` / `createdAt` / `created_at` / `added_at`，不把更新时间当加入时间。
- 原搜索包含的姓名、罗马字、账号、学校、地区、备注、标签全部保留，补充 HUI NOTE、大小写及全角字符归一化。地区、学校和标签可组合筛选，各条件可单独移除。
- ALL ARTISTS 支持 Grid / List，每批显示 24 位；所有筛选针对完整已加载数据，并非当前一页。
- 卡片可打开轻量详情；Instagram 单独作为外链出口，保留原点击记录函数。纠错按钮自动展开原有表单并选中对应艺术家。
- 加载阶段使用骨架和 `—`，不闪现 0；失败提供重试；本机缓存损坏不会中断页面。
- 原中、英、日语言切换保留，新界面三语齐全；原投稿和纠错表单移至底部，字段名、提交地址和负载保持不变。

## 不变的功能边界

以下内容与原压缩包对应 Git 基线逐段/逐文件核对一致：

- `app.js` 认证、注册、访客会话恢复、退出、点击跟踪、投稿和纠错提交处理。
- 全部认证 API、`_auth.js`、`_supabase.js`、排行榜/点击记录 API、投稿/纠错 API、后台 API。
- `admin.html`、`admin.js`、原 `i18n.js`、原样式文件、数据库脚本和 Vercel 路由。

`api/artists.js` 唯一修改是给原来的 GET 响应附加可选展示字段。数据库查询仍为 `artists?select=*&order=name.asc`；旧字段及返回值保持不变，没有数据库迁移和写入。

## HUI NOTE、Featured 和图片

`public/artist-editorial.js` 是可选的手工策展配置，当前为空。以已经存在的真实 artist ID 为键，可填写 `huiNote`、`featured`、`imageUrl`、`imageAlt`。只填 HUI 提供的短评和有权展示的图片 URL，不存放密钥。

这份配置只影响展示，不修改数据库。艺术家未配置时直接使用 API 原资料；被删除或不存在的 ID 不会额外生成艺术家。也支持未来数据库已经存在的 `hui_note`、`featured`、`image_url` 等可选字段。本次没有新增后台编辑字段，也没有把既有自动采集的 `note` 冒充 HUI 推荐语；原 `note` 在详情的“收录资料”中保留。

## AI Finder 的边界

现在只展示面板、示例描述和 Coming Soon。输入不发送，不存储，不调用任何 AI API，不需要任何模型密钥。

`public/ai-finder-service.js` 是唯一服务边界；`contracts/artist-finder.d.ts` 给出请求、响应和未来服务端 Adapter / Repository 类型。

未来的连接顺序应为：UI → 同源 `/api/ai` 或统一服务 → 服务端 Provider Adapter → Artist Repository → 数据库。响应只包含消息及真实 `artistIds`；客户端用 `ArtistIndex.resolveIds` 去重、过滤未知 ID，再复用现有卡片。不要把模型生成的姓名、学校或 Instagram 资料当数据库内容。本次没有创建实际 `/api/ai` 路由。

## 预览与验证

在项目目录运行：

```sh
npm run preview
```

打开 `http://127.0.0.1:4173`，使用原欢迎页和访客浏览入口。预览服务器只监听本机，读取线上公开的艺术家及排行榜接口；访问失败时使用压缩包中的数据快照。所有 POST/PATCH/DELETE 都会被本地阻止，不会转发账号、密码、投稿、纠错或点击写入。

**预览限制：**当前线上旧 API 尚未输出创建时间，所以本地真实数据预览里的“最近加入”会显示日期不可用提示。将新的 `api/artists.js` 一起部署后，只要原数据库行有 `created_at`，该区域会自动按实际创建时间显示。对于仍然没有日期的行，不猜日期。原压缩包快照有 308 条；本次检查时线上公开接口有 348 条，未用旧快照覆盖线上名单。

本地只读预览不提供账号创建或真实提交成功；它会明确显示未发送。完整认证回归使用项目已有的隔离测试，真实线上邮箱账号未被创建或修改。

已运行：

```sh
npm run check
npm run check-ui
```

验证包含原认证/注册/后台用户测试、语法检查、308 条旧记录兼容、组合筛选、创建时间排序、随机抽取、原 URL 保留、未知 ID 排除、零 AI 请求、可选 API 字段和原查询不变。浏览器检查使用线上 348 位真实艺术家，覆盖访客进入与刷新恢复、搜索、组合筛选及条件移除、Grid/List、24→48 分批显示、随机发现、详情/Instagram URL、纠错预选、三语及 320/390px 手机布局。

## 部署与回退

交付包保持原项目目录结构，不包含环境变量或密钥。继续使用原 Vercel 项目和既有 Supabase 环境变量，不新建数据库，不运行种子导入。

请一并部署 `public/` 的新增/修改文件和 `api/artists.js`；不应只复制 HTML 而遗漏新脚本或只发布前端而遗漏日期映射。它仍为静态前端，不需要引入前端构建工具。原 Vercel 项目设置和 `vercel.json` 无需更改。

确认预览后，按原项目流程部署。回退可使用 Vercel 上一个部署，或原压缩包中的前端及 `api/artists.js`；本次没有数据库结构变化，回退不需要恢复数据库。

## 三语翻译补齐

本次修订补齐此前仍显示英文的导航、栏目标题、网格/列表、AI 面板及准备中标记、版权说明、欢迎页辅助文案、HUI 推荐语标签和本地预览提示。它们现在随中文、日文、英文选择即时切换；HUI STUDIO、Instagram 品牌名及艺术家原始资料不改写。

已在浏览器实际验证三语切换，并确认 320px 日文导航无横向溢出，网格/列表文字仍可见。仅调整前端文案、翻译绑定和必要样式；数据库及核心功能无变化。
