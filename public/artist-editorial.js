/* Optional, hand-written HUI annotations keyed by an EXISTING artist ID.
 * Keep this object empty until HUI supplies editorial content.
 * Allowed fields: featured (boolean), huiNote (string), imageUrl (licensed
 * image URL), imageAlt (string). Missing entries leave the API data unchanged.
 * This file contains public content only; never put secrets here.
 */
const ArtistEditorial = Object.freeze({});
